{
    "tasks": [
        {
            "type": "shell",
            "label": "ninja build",
            "command": "ninja",
            "options": {
                "cwd": "${workspaceRoot}/out"
            },
            "group": "build"
        }       
    ],
    "version": "2.0.0"
}